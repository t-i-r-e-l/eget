# Инструкция по публикации проекта на GitHub

## ✅ Что уже готово

1. ✅ `go.mod` и `go.sum` - зависимости проекта
2. ✅ `.gitignore` - исключение ненужных файлов из репозитория
3. ✅ `README.md` - основная документация проекта
4. ✅ `INSTALL.md` - подробная инструкция по установке
5. ✅ `config.example.json` - пример конфигурации с предупреждениями
6. ✅ `domains.example.txt` - пример файла со списком доменов
7. ✅ `LICENSE` - MIT License

## 📝 Что нужно сделать перед публикацией

### 1. Обновить имя модуля в go.mod

Замените `github.com/yourusername/eget` на реальный путь к вашему репозиторию:

```bash
# После создания репозитория на GitHub, обновите go.mod
sed -i 's|github.com/yourusername/eget|github.com/YOUR_USERNAME/eget|g' go.mod
go mod tidy
```

### 2. Обновить ссылки в README.md

Найдите и замените все упоминания `yourusername` на ваш GitHub username:

```bash
sed -i 's/yourusername/YOUR_USERNAME/g' README.md
sed -i 's/yourusername/YOUR_USERNAME/g' INSTALL.md
```

### 3. Лицензия

✅ Лицензия уже добавлена (`LICENSE` с MIT License)

### 4. Проверить конфигурационные файлы

Убедитесь, что:
- ✅ `users.json` не попадет в репозиторий (уже в .gitignore)
- ✅ `config.example.json` содержит только примеры
- ✅ Логи не попадут в репозиторий (уже в .gitignore)

## 🚀 Публикация на GitHub

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `eget`)
3. **НЕ** инициализируйте с README, .gitignore или лицензией

### Шаг 2: Инициализируйте git (если еще не сделано)

```bash
cd /home/tirel/Рабочий\ стол/eGET_
git init
```

### Шаг 3: Добавьте файлы

```bash
git add .
git commit -m "Initial commit: eGET HTTP Tunnel for SOCKS5"
```

### Шаг 4: Подключите удаленный репозиторий

```bash
git remote add origin https://github.com/YOUR_USERNAME/eget.git
```

### Шаг 5: Отправьте код

```bash
git branch -M main
git push -u origin main
```

## 🔒 Безопасность перед публикацией

Проверьте, что в репозиторий не попадут:

- ❌ Реальные ключи шифрования
- ❌ `users.json` с реальными пользователями
- ❌ Логи с чувствительной информацией
- ❌ Личные данные

Все это уже исключено через `.gitignore`, но проверьте еще раз:

```bash
# Проверьте, что будет закоммичено
git status

# Проверьте содержимое .gitignore
cat .gitignore
```

## 📋 Чек-лист перед публикацией

- [x] Обновлено имя модуля в `go.mod` (заменено на `t-i-r-e-l`)
- [x] Обновлены ссылки в `README.md` и `INSTALL.md` (заменено на `t-i-r-e-l`)
- [x] Добавлена лицензия (MIT License)
- [x] Проверено, что чувствительные файлы в `.gitignore`
- [ ] Проект собирается без ошибок: `go build ./...`
- [x] Документация читаема и понятна
- [x] Примеры в `config.example.json` безопасны (есть предупреждения)

## 🎉 После публикации

1. Добавьте описание репозитория на GitHub
2. Добавьте теги (tags) для релизов
3. Настройте GitHub Actions для автоматической сборки (опционально)
4. Создайте первый release (опционально)

## 📝 Пример описания для GitHub

```
eGET - High-performance HTTP tunnel for SOCKS5 proxy

Tunnel your SOCKS5 traffic through HTTP/HTTPS requests with AES-GCM encryption.
Optimized for CDN and proxy servers.

Features:
- HTTP/HTTPS tunneling
- AES-GCM encryption
- SOCKS5 proxy support
- Flexible routing rules
- Multiple upstream support
- CDN optimized
```




